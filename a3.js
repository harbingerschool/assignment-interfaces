

/**
 * This function is a wrapper. Use the proper array method so the function returns 
 * the proper index of the desired name parameter.
 * 
 * @param {string} name the name you want the index of
 * @param {array} nameList the list of names
 */
function indexOf(name){
  const names = ["joe", "john", "tim", "lisa", "tina", "fred"];
  // TODO: write your code here
}

modules.export = { indexOf };